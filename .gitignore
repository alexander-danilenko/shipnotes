# Local environment file (never commit secrets).
.env

# AI Workflow internal docs.
docs/
.remember/

# GoReleaser build output (local `goreleaser build/release` runs).
dist/

# Built binary (produced by `go build -o shipnotes .`).
/shipnotes

# Editor / OS noise.
.idea/
.vscode/
.DS_Store
